// ==================== All Import
import { useEffect } from 'react'

const ScrollToTop = () => {
  return (

    // ==================== For keep scroll up
    useEffect(()=>{
        window.scrollTo(0,0)
    })
  )
}

export default ScrollToTop